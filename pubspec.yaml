name: fureverhealthy
description: "A new Flutter project."
publish_to: 'none'


version: 1.0.0+6


environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.15.1
  firebase_auth: ^5.6.2
  cloud_firestore: ^5.6.11
  firebase_storage: ^12.3.4
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8
  intl: ^0.18.1 
  http: 1.1.0 # for API calls
  flutter_dotenv: ^5.0.2
  image_picker: ^1.1.2
  google_sign_in: ^6.1.5
  flutter_facebook_auth: ^6.0.3
  twitter_login: ^4.4.0
  flutter_markdown: ^0.6.0
  file_picker: ^10.3.3
  google_generative_ai: ^0.4.3
  url_launcher: ^6.2.5
  flutter_local_notifications: ^17.2.2
  timezone: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  table_calendar: ^3.0.9 # Add this line

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  assets:
    - assets/fhlogo.png
    - assets/furever2.png
    - assets/furever.png
    - assets/facebook.png
    - assets/google.png
    - assets/twitter.png
    - assets/cat.png
    - assets/dog.png
    # - assets/feeding.png
    # - assets/community.png
    - assets/vaccination.png
    - assets/vet.png
    - assets/golden_retriever.jpeg
    - assets/puppy.jpeg
    - assets/dog_walking.jpeg
    - assets/.env
    - assets/add_post.png
    - assets/appoint.png
    - assets/userprof.png
    - assets/hmpage.png
    - assets/guide.png
    - assets/pawscomm.png
    - assets/groom.png
    - assets/feed.png
    - assets/medications.png
    - assets/settings.png
    - assets/upload.png
    - assets/schedule.png
    - assets/reminders.png
    - assets/recent_notes.png
    - assets/pet_grooming.png
    - assets/pet_feeding.png
    - assets/pet_medication.png
    - assets/pet_vaccines.png
    - assets/feeding_white.png
    - assets/grooming_white.png
    - assets/medicine_white.png
    - assets/notes_white.png
    - assets/vaccine_white.png
    - assets/notif_bell.png
    - assets/user_prof.png
    - assets/spencer.jpeg
    - assets/comment.png
    - assets/pawheart.png
    - assets/pawheart_filled.png
    - assets/save_post.png
    - assets/share_button.png
    - assets/pet_images/
    - assets/vet_doctor.png
    - assets/breeds/


  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
